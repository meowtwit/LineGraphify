# 解説レポート：ミッキーガウス

**作品タイトル**：ミッキーガウス（Mickey Gauss）  
**コンセプト**：パブリックドメインとなった《蒸気船ウィリー》（1928年）の1シーンを，複素平面上の3次多項式曲線群として再表現する。"ミッキー" と "ガウス平面" を掛け合わせた言葉遊びが題名の由来。関数グラフアートを「実軸 × 虚軸」の複素座標系で統一的に記述することで，数学的な整合性と表現の一貫性を両立した。

---

## 1. 使用した関数族

本作品では，すべての曲線を **複素数値3次多項式（媒介変数表示）** で統一した。

$$
z(t) = c_3\,t^3 + c_2\,t^2 + c_1\,t + c_0, \quad c_k \in \mathbb{C}, \quad 0 \le t \le 1
$$

実部・虚部に展開すると，各曲線セグメントは

$$
x(t) = \operatorname{Re}(z(t)), \quad y(t) = \operatorname{Im}(z(t))
$$

という **2本の3次多項式のペア** に相当する。これは課題指定の「媒介変数表示の3次多項式」と完全に対応する。

複素数表記にまとめる利点は，平行移動・回転・拡大縮小を $z \mapsto \alpha z + \beta$（$\alpha, \beta \in \mathbb{C}$）という1本の式で記述できることであり，座標変換の数学的な整理に役立つ。ただし本実装では変換処理を直交座標系で行い，出力時に係数を複素数の形にまとめている。

---

## 2. 座標変換

元の画像座標系（ピクセル，左上原点，y軸下向き）を，グラフ座標系（中心原点，y軸上向き）へ変換する。

画像サイズを $W \times H$，スケールを $s$（実軸方向の半値幅）とすると，

$$
z_{\text{graph}} = \frac{s}{W/2}\!\left[(u - W/2) - i\,(v - H/2)\right]
$$

ここで $(u, v)$ はピクセル座標，$i = \sqrt{-1}$。実装では $s = 10.0$，$W = 1200$，$H = 800$ を使用した。これを成分で書くと

$$
x = \frac{u - 600}{60}, \qquad y = -\frac{v - 400}{60}
$$

となり，$x \in [-10,\,10]$，$y \in [-6.67,\,6.67]$ の座標系に対応する。

**拡大・縮小**：スケール係数 $s/(W/2) = 1/60$ によるピクセル座標の正規化は，複素数のスカラー倍 $z \mapsto \lambda z$ に相当する。

**反転**：虚部の符号反転（$v \mapsto -v$）がy軸反転に対応する。

---

## 3. 区間制限と区分関数的な運用

1本の輪郭線は複数のセグメント（区間 $0 \le t \le 1$ を持つ3次多項式）に分割される。全体で **497 輪郭・41,385 セグメント**，および黒塗り領域 **815 個** を使用した。

各セグメントは定義域 $t \in [0,1]$ でのみ有効であり，それ以外では定義されない。これは課題の「区分関数で描きたい部分だけ表示する」に相当する：

$$
z_{j,k}(t) \text{ は } t \in [0,1] \text{ のみ有効，区間外は未定義}
$$

輪郭の重要度（長さ・面積）に応じてセグメント数を配分しており，主要輪郭（輪郭1：長さ 11,265 px・4,647 セグメント）には多くのセグメントを割り当て，小さな輪郭（輪郭494–497：長さ 25–27 px・15–17 セグメント）は最小限に抑えた。これにより，視覚的に重要な部分に精度を集中させる設計になっている。

---

## 4. 微分を使った箇所

### 4-1. Canny エッジ検出における画像輝度の偏微分

輪郭の抽出に用いた Canny エッジ検出は，画像輝度 $I(u, v)$ の**偏微分**に基づいて動作する。

$$
\nabla I = \left(\frac{\partial I}{\partial u},\ \frac{\partial I}{\partial v}\right)
$$

勾配の大きさ

$$
\|\nabla I\| = \sqrt{\left(\frac{\partial I}{\partial u}\right)^2 + \left(\frac{\partial I}{\partial v}\right)^2}
$$

が閾値（`lower`・`upper`）を超えた画素をエッジとして検出する。実装では Sobel フィルタにより偏微分を離散近似している。この微分処理が「どこに輪郭があるか」を決定するため，作品全体の基盤となっている。

パラメータ調整：`blur=10`（ノイズ除去），`lower=0`，`upper=10`（閾値），`min_contour_length=25 px` の組み合わせで，ミッキーのしっぽや指の細部まで拾いながらノイズを抑制した。

### 4-2. Catmull-Rom スプラインによる C¹ 連続接続（傾き条件）

隣接するセグメント $z_{j,k}$ と $z_{j,k+1}$ を滑らかにつなぐため，**Catmull-Rom スプライン**の接線公式を用いた。点列 $\{P_{k-1}, P_k, P_{k+1}, P_{k+2}\}$ に対して，区間 $[P_k, P_{k+1}]$ の両端の接線ベクトルを

$$
\mathbf{m}_k = \frac{P_{k+1} - P_{k-1}}{2}, \quad \mathbf{m}_{k+1} = \frac{P_{k+2} - P_k}{2}
$$

と定める。これは中心差分 $\approx dP/dk$ の有限差分近似であり，**微分の数値的な利用**に相当する。

この接線条件を満たすエルミート形式の3次多項式は

$$
z(t) = (2t^3 - 3t^2 + 1)P_k + (t^3 - 2t^2 + t)\mathbf{m}_k + (-2t^3 + 3t^2)P_{k+1} + (t^3 - t^2)\mathbf{m}_{k+1}
$$

となり，$z(0)=P_k$，$z(1)=P_{k+1}$，$z'(0)=\mathbf{m}_k$，$z'(1)=\mathbf{m}_{k+1}$ が成立する。すなわち **$z_{j,k}(1) = z_{j,k+1}(0)$（値の連続）かつ $z'_{j,k}(1) = z'_{j,k+1}(0)$（傾きの連続）** が自動的に保証される。

---

## 5. 工夫した点

**数学的工夫**

- 実軸・虚軸を $(x, y)$ に対応させることで，平面曲線を複素多項式1本で記述できる複素平面表現を採用した。「ミッキーガウス」というタイトルはこの数学構造から来ている。
- 輪郭の重要度スコア（長さ × 面積の重み付き和）に比例してセグメント数を配分し，重要な輪郭ほど高い近似精度を確保した。
- Catmull-Rom スプラインにより C¹ 連続性を自動保証し，セグメント間の折れ目を排除した。

**表現の工夫**

- 処理パイプライン（Canny エッジ検出 → 輪郭抽出 → スプライン化 → 複素多項式変換）を Python GUI ツールとして実装し，パラメータを対話的に調整しながら最終作品に至った（自動ループによるスコア評価を含む）。
- ブラー量・Canny 閾値・最小輪郭長を変化させた複数回のループで得られた F1 スコア（エッジカバレッジ × 精度）の最良値に基づき最終パラメータを選定した。
- 出力TeX ファイルにより，数式一覧を $\LaTeX$ 形式で完全に記録・再現可能な状態にした。

---

## 6. 提出物一覧

| 項目 | ファイル |
|------|---------|
| 完成作品画像 | `mickey2.png` |
| 数式一覧（TeX） | `Mickey-gauss.tex`（497 輪郭・41,385 セグメント） |
| 下絵 | `mickey.png` |
| 制作ログ | `AI_Log_Response_from_Hayakumo_Futo.md`（Claude Code），`Rollout_May_11_2026_Response_from_Hayakumo_Futo.md`（OpenAI Codex） |
| 解説レポート | 本ファイル |
