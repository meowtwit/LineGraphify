# 解説レポート ── 蒸気船ウィリーを数式で描く

## 作品概要

| 項目 | 内容 |
|------|------|
| 作品タイトル | 蒸気船ウィリー ── 数式で再現する100年前のアニメ |
| 下絵素材 | *Steamboat Willie*（1928年・Walt Disney Productions）※ 2024年1月にパブリックドメイン入り |
| 使用ツール | Python（OpenCV・NumPy・Pillow）+ 自作スクリプト |
| 総数式数 | 41,385 本（3次パラメトリック曲線）+ 塗り潰し領域 815 個 |

---

## 1. 下絵の部品分解

Steamboat Willie の静止フレームを次の4カテゴリに分解した。

| カテゴリ | 具体箇所 | 採用した関数族 |
|----------|----------|---------------|
| 外形輪郭 | ミッキーの全身シルエット、蒸気船の船体 | 3次多項式（高曲率部） |
| 細部曲線 | 目・鼻・口・耳の輪郭、指・尾 | 3次多項式（短区間） |
| 幾何構造 | 操舵輪のスポーク、円形部品 | 3次多項式（円弧近似） |
| 塗り潰し境界 | 黒領域の境界線 | 閉じたパラメトリック曲線 |

---

## 2. 使用した関数族と数式の形

### 2.1 パラメトリック3次多項式

すべての輪郭を次の形式の**パラメトリック曲線**で表現した。

$$
x(t) = a_3 t^3 + a_2 t^2 + a_1 t + a_0 \qquad (0 \le t \le 1)
$$
$$
y(t) = b_3 t^3 + b_2 t^2 + b_1 t + b_0 \qquad (0 \le t \le 1)
$$

**3次多項式を選んだ理由：**
- 4つの係数（$a_0 \sim a_3$）で始点・終点の位置と傾き方向を独立に制御できる
- 端点での微分（傾き）条件を課した場合でも線形方程式系として解ける
- 2次では曲率変化が乏しく人物輪郭の複雑さに対応できない

### 2.2 代表的な数式（実際の出力から抜粋）

ミッキーの輪郭上側（コンター1・セグメント1〜3）：

```
x(t) = -0.01668·t³ + 0.02502·t² + 0.008340·t  −6.747
y(t) =  0.008340·t³ − 0.01668·t² − 0.008340·t + 6.664

x(t) =  0.02502·t³ − 0.03336·t² + 0.008340·t  −6.731
y(t) =  0.008340·t³ − 0.008340·t² − 0.01668·t + 6.647

x(t) = −0.02502·t³ + 0.04170·t² + 0.01668·t  −6.731
y(t) = −0.01668·t³ + 0.02502·t² − 0.008340·t + 6.631
```

各セグメントの定義域はすべて $0 \le t \le 1$。

### 2.3 座標系の設計

| 設定 | 内容 |
|------|------|
| 原点 | 画像中心 |
| $x$ 軸 | 右向き正、範囲 $[-10,\ 10]$ |
| $y$ 軸 | 上向き正（画像座標の $y$ を反転）、範囲 $[-6.67,\ 6.67]$ |
| スケール | 1単位 ＝ 60 px（$1200 \div 20 = 60$） |

ピクセル座標 $(u, v)$ から数学座標 $(x, y)$ への変換：

$$
x = \frac{u - 600}{60}, \qquad y = -\frac{v - 400}{60}
$$

---

## 3. 変換の説明

### 3.1 平行移動（定数項 $a_0,\, b_0$）

各セグメントの始点 $(x_0, y_0)$ は係数 $a_0 = x_0$、$b_0 = y_0$ として表現される（$t=0$ 代入で確認可）。  
例：セグメント1の始点は $(-6.747,\ 6.664)$、画像左上付近に対応する。

### 3.2 拡大・縮小（スケーリング）

操舵輪のような円形構造は、標準的な円 $(\cos\theta, \sin\theta)$ を3次多項式で近似したうえで、中心座標への平行移動と半径スケーリングを係数に反映させた。

### 3.3 反転（$y$ 軸）

画像の $y$ 軸（下向き正）を数学座標の $y$ 軸（上向き正）に反転することで、グラフ上で視覚的に正立した作品になる。上記の変換式で $y = -v/60 + \text{const}$ が反転に相当する。

---

## 4. 定義域制限（区分関数）

1本の輪郭を複数のセグメントに分割し、各セグメントに $0 \le t \le 1$ の定義域を割り当てた。  
これにより「輪郭の一部だけ表示する」区分関数と同等の効果が得られる。

例：ミッキーの全身輪郭（コンター1）は合計 **4,647 セグメント**に分割。各セグメントは互いに接続し、大きなパラメータ $T \in [0, 4647]$ で全輪郭を一筆書き的に辿れる構造になっている。

---

## 5. 微分を使った箇所

### 5.1 Catmull-Rom スプラインによる接続点の傾き一致（C1連続性）

隣接するセグメント $i$ と $i+1$ の境界 $t=1$（セグメント $i$）と $t=0$（セグメント $i+1$）で、位置と傾きが一致するように接線ベクトルを設定した。

**位置の連続性（C0）：**
$$
x_i(1) = x_{i+1}(0), \qquad y_i(1) = y_{i+1}(0)
$$

**傾きの連続性（C1）─ Catmull-Rom の接線公式：**

制御点列 $P_{i-1},\, P_i,\, P_{i+1}$ に対して、点 $P_i$ での接線ベクトルを

$$
\mathbf{m}_i = \frac{P_{i+1} - P_{i-1}}{2}
$$

と定める。これを境界条件として代入すると、

$$
x_i'(1) = x_{i+1}'(0), \qquad y_i'(1) = y_{i+1}'(0)
$$

が自動的に成立し、曲線のつなぎ目で**傾きが滑らかに一致する**。

この条件がなければ、隣接セグメントの接続点で「折れ曲がり」が生じ、輪郭が粗くなる。実際、この条件を適用した結果、目・口・輪郭などの滑らかな曲線が自然に再現された。

---

### 5.2 最小二乗法によるフィッティング（誤差最小化の微分）

各セグメントを3次多項式で近似する際、輪郭上のサンプル点 $(x_k, y_k)$ と曲線の誤差二乗和

$$
E = \sum_{k=1}^{N} \left[ (x_k - x(t_k))^2 + (y_k - y(t_k))^2 \right]
$$

を最小化する係数 $\mathbf{c} = (a_0, a_1, a_2, a_3)$ を求めた。

最小化条件は

$$
\frac{\partial E}{\partial a_j} = 0 \quad (j = 0, 1, 2, 3)
$$

これを解くと線形方程式系（正規方程式）$A^\top A\, \mathbf{c} = A^\top \mathbf{b}$ が得られ、NumPy の `lstsq` で数値的に解いた。

**結果：** 最良セグメントで $\text{MSE} \approx 0.012\ \text{px}^2$、平均でも数 px² 以内に収まり、視覚的に十分なフィッティング精度が得られた。

---

## 6. 工夫した点

### 数学的工夫

- **7次多項式への拡張も試行**：主要輪郭（面積上位のコンター）に対して $\deg = 7$ のフィッティングを行い、1セグメントあたりの式数を削減しながら精度を維持できることを確認した（最終提出では3次を採用）。
- **奇数次元パディング**：動画出力時に libx264 が奇数幅・高さを拒否する問題に対し、`pad=ceil(iw/2)·2:ceil(ih/2)·2` フィルタで偶数にパディングした。

### 表現の工夫

- **塗り潰し領域の分離**：輪郭だけでなく黒塗り領域をクローズドパスとして独立抽出し、815個の塗り潰しゾーンを重ねることでオリジナルの白黒表現を忠実に再現した。
- **輪郭の長さ・面積によるセグメント数の配分**：輪郭の長さが長いほど多くのセグメントを割り当て、細部の短い輪郭には少数のセグメントを使うことで、全体41,385本の数式を効率的に分配した。

---

## 7. まとめ

本作品では、1928年制作のアニメーション *Steamboat Willie* の1フレームを、**3次パラメトリック多項式 41,385 本**で表現した。輪郭検出・最小二乗フィッティング・Catmull-Rom スプラインという3つの数学的道具を組み合わせることで、手動では不可能な精度の「数式画」を実現した。

微分の活用は「接続条件（C1連続性）」と「誤差最小化（正規方程式）」の2箇所が中心であり、どちらも視覚的な品質に直結している。
