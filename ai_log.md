# 制作ログ ── 生成系 AI（Claude）との対話記録

> 使用AI：Claude（Anthropic / claude-sonnet-4-6）  
> 制作期間：2026年5月  
> 目的：Steamboat Willie の静止フレームを数式のみで再現する  
> ※ 以下は実際の対話から制作に関係する部分を抜粋・整形したものです。

---

## Step 1 ── アプローチの設計

**自分：**  
写真や絵を関数のグラフだけで再現するプログラムを作りたい。手動で一本ずつ式を作るのは無理なので自動化したい。どうすればいい？

**Claude：**  
大きく2段階に分けるといいです。

1. **輪郭検出**：画像の輪郭を Canny エッジ検出で抽出し、連続した点列として取り出す。
2. **多項式フィッティング**：その点列を区間 [0, 1] のパラメータ t で走る 3次多項式 x(t), y(t) に最小二乗でフィッティングする。

この2ステップで「画像 → 数式の集合」に変換できます。Python の OpenCV と NumPy があれば実装できます。

---

**自分：**  
Canny エッジ検出って何ですか？

**Claude：**  
画像の輝度 I(x,y) の**偏微分**を使って輪郭を検出するアルゴリズムです。

∇I = (∂I/∂x, ∂I/∂y)

この勾配ベクトルの大きさが大きい場所 ＝ 明暗が急変する場所 ＝ 輪郭、という仕組みです。  
実装上は Sobel フィルタという離散近似で計算しています。

処理の流れ：
1. ガウシアンフィルタでノイズ除去（ぼかし）
2. x・y 方向の輝度勾配を計算
3. 勾配の強さが閾値を超えた画素をエッジとして選ぶ（lower・upper で調整）
4. 細線化（non-maximum suppression）で1px幅にする

---

## Step 2 ── 3次多項式を選んだ理由

**自分：**  
3次多項式にする理由は？2次じゃだめですか？

**Claude：**  
2次多項式 at² + bt + c だと係数が3個しかなく、端点での傾き（微分値）を指定できません。

3次 at³ + bt² + ct + d にすると係数が4個になり：
- 始点の位置（t=0）
- 終点の位置（t=1）
- 始点の傾き（t=0 での微分）
- 終点の傾き（t=1 での微分）

この4条件を同時に課せます。これが曲線を「滑らかにつなぐ」のに必要な最低次数です。

---

## Step 3 ── つなぎ目のガタガタを修正

**自分：**  
実装したら曲線のつなぎ目がガタガタになった。どうすれば滑らかになりますか？

**Claude：**  
隣接セグメント間で「傾き（接線ベクトル）が一致していない」のが原因です。**Catmull-Rom スプライン**を使うと解決できます。

制御点列 P_{i-1}, P_i, P_{i+1} があるとき、点 P_i での接線ベクトルを

  m_i = (P_{i+1} - P_{i-1}) / 2

と定義します。この接線をセグメントの端点条件として使うと、隣接セグメントの境界で

  dx_i/dt |_{t=1} = dx_{i+1}/dt |_{t=0}

が自動的に成立し、つなぎ目が滑らかになります（C1連続）。コードでは：

```python
b1 = p1 + (p2 - p0) / 6.0  # 制御点（前向き）
b2 = p2 - (p3 - p1) / 6.0  # 制御点（後向き）
```

この `(p2 - p0) / 6.0` が有限差分による微分の近似です。

---

## Step 4 ── フィッティング精度の評価

**自分：**  
フィッティングの精度ってどうやって測りますか？

**Claude：**  
MSE（平均二乗誤差）が標準的です。

  E = (1/N) Σ [(x_k - x(t_k))² + (y_k - y(t_k))²]

これを最小化する係数を求めるのが最小二乗法で、条件は

  ∂E/∂a_j = 0  (j = 0, 1, 2, 3)

これを解くと正規方程式 AᵀA c = Aᵀb が得られ、NumPy の `lstsq` で数値的に解けます。

ミッキーの輪郭では最良セグメントで MSE ≈ 0.012 px² ──ほぼ誤差ゼロに近い精度です。

---

## Step 5 ── パラメータ調整

**自分：**  
Canny の閾値を lower=0, upper=10 にしたらかなり細部まで取れましたが、ノイズも増えました。

**Claude：**  
正常な挙動です。閾値を下げると弱いエッジも検出するので細部が取れる一方、短いノイズ輪郭も増えます。`min_contour_length`（最小輪郭長さ）で短すぎる輪郭を除外するフィルタを入れると整理できます。

今回の設定（min_contour_length=25px、blur=10）でノイズを除きつつ、ミッキーのしっぽや指の線まで拾えています。

---

## Step 6 ── 多様な関数族の試行

**自分：**  
3次多項式だけでなく、指数・対数・三角・双曲線関数なども使ってみたいです。

**Claude：**  
可能です。次の関数族を実装できます。

| 種類 | 式の形 | 向いている形状 |
|---|---|---|
| 3次多項式 | a + bt + ct² + dt³ | なめらかな一般曲線 |
| フーリエ（2次） | a + b sin(πt) + c cos(πt) + … | 周期的・円弧 |
| 双曲線 | a + b sinh(t) + c(cosh(t)−1) + … | 急峻な立ち上がり |
| 指数・対数 | a + beᵗ + c ln(t+1) + … | 非対称な曲線 |

各セグメントに全基底を最小二乗フィットして、最もMSEが小さいものを自動選択する仕組みにします。

---

**自分（後日）：**  
多様な関数を使う方針はかなり失敗になりました。フィッティング品質が3次多項式と変わらない、または悪化するケースが多く出力も不安定でした。

**自分：**  
一旦たくさんの関数を使うのはストップします。以前の3次でやってたやつに戻せますか？

**Claude：**  
戻しました。多関数の試みは `multi-basis` ブランチに保存してあります。  
数値的安定性と解釈のしやすさの観点から、3次多項式が今のスケールには最も適切でした。

---

## Step 7 ── 最終出力の確認

**自分：**  
最終的に何本の数式になりましたか？

**Claude：**  

| 項目 | 数値 |
|---|---|
| 3次パラメトリック曲線 | 41,385 本 |
| 塗り潰し領域 | 815 個 |
| 最大輪郭のセグメント数 | 4,647 本 |
| 処理画像サイズ | 1,200 × 800 px |
| 座標範囲 | x ∈ [−10, 10]、y ∈ [−6.67, 6.67] |

手作業では不可能な規模ですが、最小二乗法と Catmull-Rom の組み合わせで元の画像と視覚的に遜色ない再現が得られました。

---

## 自分の意思決定メモ

- **3次 vs 高次の選択**：7次多項式・フーリエ・双曲線なども試したが、3次が最も安定で数学的説明がしやすいため最終採用した。
- **塗り潰しの採用**：輪郭線のみでは「スケッチ」になる。Steamboat Willie の白黒表現を再現するために815個の閉じた塗り潰し領域を追加した。
- **Canny 閾値の選択**：lower=20, upper=80 と lower=0, upper=10 を試し、後者のほうがしっぽや指の細部まで拾えたため採用した。

---

*このログは Claude（claude-sonnet-4-6）との実際の対話をもとに抜粋・整形したものです。*
